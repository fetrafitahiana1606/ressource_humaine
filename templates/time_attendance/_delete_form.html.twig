<form method="post" action="{{ path('app_time_attendance_delete', {'id': time_attendance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ time_attendance.id) }}">
    <button class="btn">Delete</button>
</form>
