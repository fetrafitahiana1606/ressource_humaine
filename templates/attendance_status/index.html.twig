{% extends 'base.html.twig' %}

{% block title %}AttendanceStatus index{% endblock %}

{% block body %}
    <h1>AttendanceStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for attendance_status in attendance_statuses %}
            <tr>
                <td>{{ attendance_status.id }}</td>
                <td>{{ attendance_status.name }}</td>
                <td>
                    <a href="{{ path('app_attendance_status_show', {'id': attendance_status.id}) }}">show</a>
                    <a href="{{ path('app_attendance_status_edit', {'id': attendance_status.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_attendance_status_new') }}">Create new</a>
{% endblock %}
