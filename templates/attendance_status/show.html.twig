{% extends 'base.html.twig' %}

{% block title %}AttendanceStatus{% endblock %}

{% block body %}
    <h1>AttendanceStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attendance_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attendance_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attendance_status_index') }}">back to list</a>

    <a href="{{ path('app_attendance_status_edit', {'id': attendance_status.id}) }}">edit</a>

    {{ include('attendance_status/_delete_form.html.twig') }}
{% endblock %}
